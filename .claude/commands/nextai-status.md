---
description: Update feature status (block, retry count)
---

# NextAI Status

Run the NextAI CLI command: `nextai status`

If arguments are provided via $ARGUMENTS, parse them and pass to the command.

## Instructions
1. Run: `nextai status $ARGUMENTS`
2. Display the output to the user
3. Suggest next steps based on the result
